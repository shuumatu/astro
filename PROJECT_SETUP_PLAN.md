# 天文科普学习平台搭建计划

## 1. 项目目标

建设一个面向天文初学者和青少年的多语言科普学习平台，核心闭环为：

~~~text
天体图鉴 → 分级任务 → 星图辅助 → 学习/观察记录 → 进度与知识图谱
~~~

本项目暂不包含活动报名、签到、团体管理和活动方发章功能。

### 目录边界

~~~text
frontend/   Vue 3 前端，独立依赖和构建产物
backend/    Spring Cloud 后端，独立 Maven 聚合工程
contracts/  前后端共享的 API 契约
deploy/     本地基础设施和部署配置
~~~

前端只通过 Gateway 调用后端；两个工程不共享源代码、构建工具或运行时依赖。

## 2. 技术决策

### 前端

- Vue 3、TypeScript、Vite、Pinia、Vue Router
- vue-i18n 负责界面文案和语言切换
- 公共科普页面后续评估迁移到 Nuxt 3，以支持 SSR/SSG 和多语言 SEO
- 首版星图使用 SVG；需要大量星体或流畅缩放时再引入 Canvas/WebGL

### 后端

- Java 17 兼容基线，生产环境推荐 Java 21
- Spring Boot 3.x、Spring Cloud Gateway
- 前端和后端分别放在根目录的 frontend、backend 文件夹中，独立安装依赖、构建和部署
- Nacos、消息队列和集中式配置暂不写死到业务代码，待本地基础设施确定后接入
- PostgreSQL 作为业务数据库，Redis 作为缓存和短期状态存储
- MinIO/COS/OSS 保存图片和观察附件

### 服务边界

| 服务 | 责任 | 首版数据边界 |
| --- | --- | --- |
| api-gateway | 统一入口、路由、鉴权前置、限流 | 无业务库 |
| identity-service | 用户、登录、权限、偏好语言 | identity_db |
| content-service | 天体图鉴、文章、任务、题库、多语言内容 | content_db |
| learning-service | 学习进度、收藏、成就、观察笔记 | learning_db |
| astronomy-service | 星表、坐标计算、星图数据接口 | astronomy_db |
| media-service | 媒体元数据和对象存储适配 | media_db |

首版不拆分任务、题库和成就服务，避免过多跨服务调用。

## 3. 多语言设计

- 使用 zh-CN、zh-TW、en、ja、ko 等 BCP 47 语言代码。
- 固定界面文案放在前端 locale 文件中，按模块懒加载。
- 天体、文章、任务使用主表 + 翻译表，不能使用 name_cn/name_en 这种列扩张方案。
- 服务之间只传稳定 ID、数值和错误码；名称、科普文案由内容服务按语言返回。
- 翻译支持草稿、审核、发布和原文版本追踪。
- 公共页面使用带语言前缀的 URL，并补充 hreflang、多语言 sitemap 和回退语言提示。

## 4. 阶段计划

### 阶段 0：工程基础（当前）

- 完成单仓库目录、Maven 聚合工程和 Vue 入口。
- 完成服务端口、网关路由、基础设施 Compose 配置。
- 建立 OpenAPI 契约目录和前后端独立构建边界。

### 阶段 1：内容与学习闭环

- 天体图鉴、分类、搜索和详情页。
- 任务列表、任务步骤、答题和完成规则。
- 用户收藏、学习进度和观察笔记。
- 内容管理端和多语言翻译工作流。

### 阶段 2：星图能力

- 根据时间、地点返回星图对象和星座连线。
- 太阳、月球、行星和恒星坐标计算。
- 星等筛选、星座线、标签、目标高亮和天体详情跳转。
- 浏览器端 Web Worker，避免星图计算阻塞 UI。

### 阶段 3：异步化和可观测性

- 使用事件驱动成就、统计和搜索索引更新。
- 增加 OpenTelemetry、Prometheus、Grafana 和集中日志。
- 增加幂等、Outbox、重试和死信处理。

### 阶段 4：性能与规模化

- 根据真实性能数据优化星图计算、星表分块和浏览器渲染。
- 增加缓存、批处理、服务水平扩展和容量压测。
- 当前阶段不引入 C++，优先保持 Java/TypeScript 技术栈的一致性。

## 5. 关键工程规则

- 每个服务拥有自己的数据和 Flyway 迁移，不跨服务直接读表。
- 前端只访问 Gateway，不直接访问内部服务。
- 首版服务接口统一使用 REST/JSON 和 OpenAPI。
- 跨服务状态更新使用事件最终一致性，首版不引入分布式事务。
- 天文学术内容必须保留来源、版本和审核记录。
- 业务 ID 与语言无关，不能使用中文名称作为主键或关联键。

## 6. 首个可验证版本

首版范围控制为：

- 30～50 个天体知识卡片
- 5～10 个分级学习任务
- 收藏和进度页
- 观察记录页
- 一个可查询的深圳默认星图
- 简体中文和英文界面

先验证学习完成率、回访率和内容生产效率，再扩展星表、语言和计算能力。
